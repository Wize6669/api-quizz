model Simulator {
    id                  String     @id @default(uuid())
    name                String
    password            String
    duration            Int
    navigate            Boolean
    Question            Question[]
    number_of_questions Int        @default(0)
}
